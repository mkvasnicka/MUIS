TODO
====

* Add support for reading files from IS:
  * list files in a folder
    * the IS API may be off due to IS load
    * should I fall back to scraping HTML? (it always works)
  * read a file (and write it to disk)
  * write file to IS